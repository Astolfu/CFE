// En tu whatsappService.js del frontend - ELIMINAR o comentar
class WhatsAppService {
    constructor() {
        this.baseUrl = import.meta.env.VITE_BACKEND_URL || 'http://localhost:3001/api';
    }

    // COMENTA ESTAS FUNCIONES o ELIMÍNALAS
    /*
    async sendAlert(contactId, tripleId) {
        // ... código ...
    }

    async sendTestMessage(contactId) {
        // ... código ...
    }
    */
}

// COMENTA esta exportación
// export const whatsappService = new WhatsAppService();